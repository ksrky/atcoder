#include<bits/stdc++.h>

#define rep(i, n) for(int i=0;i<n;++i)
using namespace std;
typedef long long ll;

int main() {
        char a;
        cin >> a;
        if (a - 'a' >= 0) cout << 'a' << endl;
        else cout << 'A' << endl;
}